# Identical fixture, one flag apart: the built-in kyverno adapter (with its
# KubeContext field wiring) must produce the same report as the shell adapter's
# env-var wiring. Enforced by review-expected.sh modulo the case names and each
# linter's identity (lint-kyverno#1 here, lint#1 there - different flags), which the
# per-case checks.grep pin exactly.
lint-cr-policy
