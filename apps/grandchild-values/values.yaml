# Values for the depth-2 grandchild-ref Application, in a directory that is NOT
# any app's source path. Nothing here can be matched by path, so a change to this
# file reaches its consumer only through argocdf's $ref value-file matcher
# (App.helmValueFilesAffected), which resolves $vals/... against the app's ref
# source. That is what case/grandchild-values-ref pins.
greeting: from-grandchild-ref
